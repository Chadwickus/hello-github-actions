name:  Run a Windows batch file

on:  push

jobs:

  build:
  
    name:  Hello world action
    
    runs-on: windows-latest
    
    steps:
      - uses:  actions/checkout@v1
     
      
      - name:  Test local api
        run: |
          action-a/test.bat
          
